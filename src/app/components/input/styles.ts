import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    backgroundColor: "#fff",
    height: 48,
    width: "100%",
    borderRadius: 8,
    borderWidth: 1,
    borderColor: "#C3C5CB",
    paddingHorizontal: 8,
  },
});
